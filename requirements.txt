fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
python-multipart==0.0.20
python-dotenv==1.2.3
Pillow>=12.0.0,<13
lxml==6.0.3
# PicImageSearch нужен ТОЛЬКО для Google Lens и импортируется лениво:
# поиск по Yandex работает через собственный клиент app/yandex_cbir.py.
# Ставится отдельно с --no-deps, потому что его метаданные требуют lxml<6,
# а на Python 3.14 нужен более новый wheel:
#   pip install "lxml==6.0.3"
#   pip install --no-deps "PicImageSearch==3.12.11"
#   pip install "pyquery>=2.0.1" "cssselect>=1.5.0" "httpx[http2]>=0.28.1"
